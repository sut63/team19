// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team19/app/ent/classdate"
)

// Classdate is the model entity for the Classdate schema.
type Classdate struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DAY holds the value of the "DAY" field.
	DAY string `json:"DAY,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ClassdateQuery when eager-loading is set.
	Edges ClassdateEdges `json:"edges"`
}

// ClassdateEdges holds the relations/edges for other nodes in the graph.
type ClassdateEdges struct {
	// Courseclasses holds the value of the courseclasses edge.
	Courseclasses []*Courseclass
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CourseclassesOrErr returns the Courseclasses value or an error if the edge
// was not loaded in eager-loading.
func (e ClassdateEdges) CourseclassesOrErr() ([]*Courseclass, error) {
	if e.loadedTypes[0] {
		return e.Courseclasses, nil
	}
	return nil, &NotLoadedError{edge: "courseclasses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Classdate) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // DAY
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Classdate fields.
func (c *Classdate) assignValues(values ...interface{}) error {
	if m, n := len(values), len(classdate.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	c.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field DAY", values[0])
	} else if value.Valid {
		c.DAY = value.String
	}
	return nil
}

// QueryCourseclasses queries the courseclasses edge of the Classdate.
func (c *Classdate) QueryCourseclasses() *CourseclassQuery {
	return (&ClassdateClient{config: c.config}).QueryCourseclasses(c)
}

// Update returns a builder for updating this Classdate.
// Note that, you need to call Classdate.Unwrap() before calling this method, if this Classdate
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Classdate) Update() *ClassdateUpdateOne {
	return (&ClassdateClient{config: c.config}).UpdateOne(c)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (c *Classdate) Unwrap() *Classdate {
	tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Classdate is not a transactional entity")
	}
	c.config.driver = tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Classdate) String() string {
	var builder strings.Builder
	builder.WriteString("Classdate(")
	builder.WriteString(fmt.Sprintf("id=%v", c.ID))
	builder.WriteString(", DAY=")
	builder.WriteString(c.DAY)
	builder.WriteByte(')')
	return builder.String()
}

// Classdates is a parsable slice of Classdate.
type Classdates []*Classdate

func (c Classdates) config(cfg config) {
	for _i := range c {
		c[_i].config = cfg
	}
}
